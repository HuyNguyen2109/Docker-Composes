apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: s3-client-credentials-eso
  namespace: administrator-apps
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: s3-client-credentials-eso
    creationPolicy: Owner
  data:
    - secretKey: s3-client-id
      remoteRef:
        key: docker-secrets
        property: authentik-s3-access-key
    - secretKey: s3-client-secret
      remoteRef:
        key: docker-secrets
        property: authentik-s3-secret-key

